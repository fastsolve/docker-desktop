os: linux
dist: trusty
language: python
services: docker

env:
  global:
  - BRANCH=$TRAVIS_BRANCH BASE=compdatasci/octave-desktop:latest

before_script:
  - docker --version
  - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_USERPASS"
  - docker pull $BASE

script:
  # Build only if the commit range of a push contains Dockerfile or .travis.yml
  - '[[ "$TRAVIS_EVENT_TYPE" == "push" && -n "$TRAVIS_COMMIT_RANGE" &&
        -n $(git log --since $(docker inspect $BASE | grep Created | perl -e "s/.+\"([\d-:T]+).+$/\$1/g" -p)) &&
        -z "$(git diff --name-only $TRAVIS_COMMIT_RANGE |
            grep -e Dockerfile -e .travis.yml)" ]] ||
     (docker build --tag fastsolve/desktop:$BRANCH . &&
      docker push fastsolve/desktop:$BRANCH &&
      touch success)'
  # Test scripts
  - git clone https://github.com/fastsolve/docker-desktop master
  - master/fastsolve_desktop.py -d -n -t $BRANCH && docker stop $(docker ps -q)
  - master/fastsolve_jupyter.py -d -n -t $BRANCH && docker stop $(docker ps -q)
  - docker images
